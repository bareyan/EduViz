"""
Generation Stage Constants

Default values and configurations specific to the generation stages
(choreography, implementation, refinement).
"""

# Default theme code (for lookups/function signatures)
DEFAULT_THEME_CODE = "3b1b"

# Default theme description (for prompt context)
DEFAULT_THEME = "3b1b dark educational style"

# Default visual hints message
DEFAULT_VISUAL_HINTS = "No explicit visual hints"

# Default section title
DEFAULT_SECTION_TITLE = "Untitled"

# Default language code
DEFAULT_LANGUAGE = "en"
